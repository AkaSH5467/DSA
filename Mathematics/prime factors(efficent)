
#include <bits/stdc++.h>

using namespace std;

void factors(int n)
{
    int flag=0;
    if(n==1)
    {
        cout<<"1"<<endl;
    }
    
    for(int i=2;i*i<=n;i++)
    {
        while(n%i==0)
        {
            cout<<i<<endl;
            n=n/i;
        }
    }
}

int main()
{
    int n;
    cin>>n;
    
    factors(n);

}
